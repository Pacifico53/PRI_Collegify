extends ../layout

block content
  .w3-card-4
    if lista.length > 0
      .w3-container
        table(class="w3-table-all")
          tr
            th Username
            th Nome
            th Email
            th Nivel
            th Curso
            th Data registo
            th Data Last
            th Filiaçao
          each a in lista
            tr
              td
                a(href='/users/'+a.username) #{a.username}
              td= a.name
              td= a.email
              td= a.level
              td= a.curso
              td= a.dateRegister.toLocaleString('pt-PT')
              td= a.dateLast.toLocaleString('pt-PT')
              td= a.affiliation
    else
      div(class="w3-container")
        p There are no users...
    .w3-container.w3-deep-orange.w3-center
      a.w3-btn.w3-hover-orange.w3-sand.w3-round-xxlarge.w3-margin(href="/signup") Registar Utilizador
      a.w3-btn.w3-hover-orange.w3-sand.w3-round-xxlarge.w3-margin(href="/") Voltar a homepage