extends ../layout

block content
  .w3-container.w3-khaki
    h1.w3-bold [#{post.type}] #{post.title}
  .w3-container.w3-sand.w3-margin.w3-border
    h2 #{post.title}
    h3 #{post.subtitle}
    p #{post.description}
    p #{post.dateRegister}
    p
      a(href='/posts/download/' + post.filename) DOWNLOAD
  .w3-container.w3-khaki
    h3 Comentarios
  
